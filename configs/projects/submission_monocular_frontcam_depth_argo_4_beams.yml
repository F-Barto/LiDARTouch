includes:
  - base.yml

project_name: SubmissionFrontCamMonocularDepthArgo_4_beams # will also be used as wandb project name
experiment_name: self_supervised # will also be used as wandb experiment name, each run have a unique id

