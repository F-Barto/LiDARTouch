input_channels: 3

network:
  tri_checkpoint_path: ???
  depth_net:
    name: GNS
    options:
      weight_ks: 1
  pose_net:
    name: monodepth2
    options:
      version: 18
      activation: elu
      input_channels: ${input_channels}